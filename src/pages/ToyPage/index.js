import React from 'react';
import { useHistory } from "react-router-dom";

import './ToyPage.scss';
import { MenuBox, NeonSign } from '../../components';

const ToyPage = () => {
	const history = useHistory();
	
	return (
		<section className='page toy-page'>
			toy
		</section>
	);
}

export default ToyPage;